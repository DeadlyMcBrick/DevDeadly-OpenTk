////Add a interface to change diff things from the engine with some controllers 
////add a format to being able to change the GUI from each interface
//add an fps counter to imguinew Opentk 9


//using ImGuiNET;

//ImGui.CreateContext();
//ImGui.GetIO().ConfigFlags |= ImGuiConfigFlags.DockingEnable;
//ImGui.GetIO().Fonts.AddFontDefault();  // Default font for ImGui
//ImGui.StyleColorsDark();  // O